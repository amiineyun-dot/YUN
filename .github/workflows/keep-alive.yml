name: Supabase Keep-Alive

on:
  schedule:
    - cron: '0 */8 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase
        run: |
          curl -s "https://rydgzqfqxsjerqkvdmdg.supabase.co/rest/v1/products?select=id&limit=1" \
            -H "apikey: sb_publishable_18H-x7wam3OAI--DP3Zq3Q_Lv3Pped4" \
            -H "Authorization: Bearer sb_publishable_18H-x7wam3OAI--DP3Zq3Q_Lv3Pped4"
          echo "Pinged at $(date -u)"
